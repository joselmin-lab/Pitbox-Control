# iOS scaffold

La Fase 1 deja preparado el espacio de iOS dentro del repositorio. Cuando el equipo ejecute Flutter 3.x en un entorno con SDK instalado, el runner nativo puede regenerarse con `flutter create --platforms=ios .` si se requiere completar los artefactos nativos estándar.
